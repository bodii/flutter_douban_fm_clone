import 'package:flutter/material.dart';

class CollectAlbumPage extends StatelessWidget {
  const CollectAlbumPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Text('this is collect album page.'),
    );
  }
}
