import 'package:flutter/material.dart';

class CollectArtistPage extends StatelessWidget {
  const CollectArtistPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Text('this is collect artist page.'),
    );
  }
}
