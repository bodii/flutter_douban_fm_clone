import 'package:flutter/material.dart';

class CollectPlayListPage extends StatelessWidget {
  const CollectPlayListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Text('this is collect play list page.'),
    );
  }
}
