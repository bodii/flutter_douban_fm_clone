import 'package:flutter/material.dart';

class CollectEditPage extends StatelessWidget {
  const CollectEditPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Text('this is collect edit page'),
    );
  }
}
